Starting the dev server (port 5180):

    npm run dev -- --port 5180

Then open http://localhost:5180 in your browser.

To run in the background (keeps terminal free):

    npm run dev -- --port 5180 &

Stopping the server:

    kill $(lsof -ti:5180)
